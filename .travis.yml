language: generic

sudo: required
services:
    - docker

before_install:
    - docker build -t isrvo/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true isrvo/docker-react npm run test